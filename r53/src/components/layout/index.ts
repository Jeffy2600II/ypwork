'use client';

/**
 * ============================================================
 * YP WORK - Layout - Barrel Export (r48)
 * ============================================================
 * "Public Airlock" ของ layout module — app shell + navigation
 *
 * Files:
 *   app-shell.tsx - AppShell component (top bar + bottom nav + FAB)
 * ============================================================
 */

export { AppShell } from './app-shell';
export type { AppShellProps, AppShellActiveNav } from './app-shell';
